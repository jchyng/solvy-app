export default function HomePage() {
  return (
    <main className="flex flex-col items-center justify-center min-h-dvh p-4">
      <h1 style={{ fontFamily: 'var(--font-serif)', fontSize: 'var(--text-brand)', color: 'var(--accent)' }}>
        Solvy
      </h1>
      <p style={{ color: 'var(--ink-3)', fontSize: 'var(--text-small)' }}>
        Week 1 skeleton — 기능 준비 중
      </p>
    </main>
  )
}
